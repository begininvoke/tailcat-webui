// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ca-x/tailcat-webui/ent/sharefile"
	"github.com/ca-x/tailcat-webui/ent/transfershare"
	"github.com/ca-x/tailcat-webui/ent/user"
)

// ShareFile is the model entity for the ShareFile schema.
type ShareFile struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID string `json:"user_id,omitempty"`
	// ShareID holds the value of the "share_id" field.
	ShareID string `json:"share_id,omitempty"`
	// StorageName holds the value of the "storage_name" field.
	StorageName string `json:"-"`
	// VirtualPath holds the value of the "virtual_path" field.
	VirtualPath string `json:"virtual_path,omitempty"`
	// SizeBytes holds the value of the "size_bytes" field.
	SizeBytes int64 `json:"size_bytes,omitempty"`
	// Mtime holds the value of the "mtime" field.
	Mtime time.Time `json:"mtime,omitempty"`
	// Blake3 holds the value of the "blake3" field.
	Blake3 string `json:"blake3,omitempty"`
	// BlockSize holds the value of the "block_size" field.
	BlockSize int64 `json:"block_size,omitempty"`
	// BlockHashes holds the value of the "block_hashes" field.
	BlockHashes []string `json:"block_hashes,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ShareFileQuery when eager-loading is set.
	Edges        ShareFileEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ShareFileEdges holds the relations/edges for other nodes in the graph.
type ShareFileEdges struct {
	// Owner holds the value of the owner edge.
	Owner *User `json:"owner,omitempty"`
	// Share holds the value of the share edge.
	Share *TransferShare `json:"share,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// OwnerOrErr returns the Owner value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShareFileEdges) OwnerOrErr() (*User, error) {
	if e.Owner != nil {
		return e.Owner, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "owner"}
}

// ShareOrErr returns the Share value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShareFileEdges) ShareOrErr() (*TransferShare, error) {
	if e.Share != nil {
		return e.Share, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: transfershare.Label}
	}
	return nil, &NotLoadedError{edge: "share"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ShareFile) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sharefile.FieldBlockHashes:
			values[i] = new([]byte)
		case sharefile.FieldSizeBytes, sharefile.FieldBlockSize:
			values[i] = new(sql.NullInt64)
		case sharefile.FieldID, sharefile.FieldUserID, sharefile.FieldShareID, sharefile.FieldStorageName, sharefile.FieldVirtualPath, sharefile.FieldBlake3:
			values[i] = new(sql.NullString)
		case sharefile.FieldMtime, sharefile.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ShareFile fields.
func (_m *ShareFile) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sharefile.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case sharefile.FieldUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				_m.UserID = value.String
			}
		case sharefile.FieldShareID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field share_id", values[i])
			} else if value.Valid {
				_m.ShareID = value.String
			}
		case sharefile.FieldStorageName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field storage_name", values[i])
			} else if value.Valid {
				_m.StorageName = value.String
			}
		case sharefile.FieldVirtualPath:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field virtual_path", values[i])
			} else if value.Valid {
				_m.VirtualPath = value.String
			}
		case sharefile.FieldSizeBytes:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field size_bytes", values[i])
			} else if value.Valid {
				_m.SizeBytes = value.Int64
			}
		case sharefile.FieldMtime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field mtime", values[i])
			} else if value.Valid {
				_m.Mtime = value.Time
			}
		case sharefile.FieldBlake3:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field blake3", values[i])
			} else if value.Valid {
				_m.Blake3 = value.String
			}
		case sharefile.FieldBlockSize:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field block_size", values[i])
			} else if value.Valid {
				_m.BlockSize = value.Int64
			}
		case sharefile.FieldBlockHashes:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field block_hashes", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.BlockHashes); err != nil {
					return fmt.Errorf("unmarshal field block_hashes: %w", err)
				}
			}
		case sharefile.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ShareFile.
// This includes values selected through modifiers, order, etc.
func (_m *ShareFile) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryOwner queries the "owner" edge of the ShareFile entity.
func (_m *ShareFile) QueryOwner() *UserQuery {
	return NewShareFileClient(_m.config).QueryOwner(_m)
}

// QueryShare queries the "share" edge of the ShareFile entity.
func (_m *ShareFile) QueryShare() *TransferShareQuery {
	return NewShareFileClient(_m.config).QueryShare(_m)
}

// Update returns a builder for updating this ShareFile.
// Note that you need to call ShareFile.Unwrap() before calling this method if this ShareFile
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ShareFile) Update() *ShareFileUpdateOne {
	return NewShareFileClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ShareFile entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ShareFile) Unwrap() *ShareFile {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ShareFile is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ShareFile) String() string {
	var builder strings.Builder
	builder.WriteString("ShareFile(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("user_id=")
	builder.WriteString(_m.UserID)
	builder.WriteString(", ")
	builder.WriteString("share_id=")
	builder.WriteString(_m.ShareID)
	builder.WriteString(", ")
	builder.WriteString("storage_name=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("virtual_path=")
	builder.WriteString(_m.VirtualPath)
	builder.WriteString(", ")
	builder.WriteString("size_bytes=")
	builder.WriteString(fmt.Sprintf("%v", _m.SizeBytes))
	builder.WriteString(", ")
	builder.WriteString("mtime=")
	builder.WriteString(_m.Mtime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("blake3=")
	builder.WriteString(_m.Blake3)
	builder.WriteString(", ")
	builder.WriteString("block_size=")
	builder.WriteString(fmt.Sprintf("%v", _m.BlockSize))
	builder.WriteString(", ")
	builder.WriteString("block_hashes=")
	builder.WriteString(fmt.Sprintf("%v", _m.BlockHashes))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ShareFiles is a parsable slice of ShareFile.
type ShareFiles []*ShareFile
